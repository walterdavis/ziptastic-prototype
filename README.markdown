#Ziptastic example with Prototype.js

Use the Ziptastic service with Prototype.js code. Apply the `zip` class to your Zip code field, and `city`, `state`, or `country` classes (optionally) to the other form fields.